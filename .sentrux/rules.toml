# Grab architectural guardrails — Task 1, foundation; cycles broken to 0
# by the engine-cycle refactor (slices A-D: media_types, engine_msg,
# file_names, runtime/net_types/ui_util leaves).
# Baseline 2026-09-23: quality 4773 after rescan (was 4708), bottleneck modularity.
# Former cycles (now broken): download<->video via VideoSource/EngineMsg,
# download<->torrent via magnet/archive + EngineMsg, window<->install_help
# via close_on_click. Edges that remain are one-way by design (UI drives
# engine; resolve/probe helpers flow download->video; cookies flow outward).
# God files present (video.rs 5.3k, download.rs 4.5k, window.rs 3.1k) so no_god_files stays off.

[constraints]
max_cycles = 0
